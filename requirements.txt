PyMuPDF==1.27.2.3 # Requires glibc 2.28+ (manylinux_2_28), works with Lambda Python 3.12 (AL2023)
pymupdf4llm==1.27.2.3 # Must version-match PyMuPDF. 0.0.18-0.2.9 silently dropped text inside
                      # vector-graphics clusters / Skia shadow images (Chrome-generated PDFs);
                      # fixed upstream in 1.27.2.x. Do NOT bump to 1.28.0: it drops table cells
                      # (incl. dollar amounts) on Skia-generated invoices — verify with
                      # tests/test_extraction_regression.py before any future bump.
boto3==1.34.0
# Note: Lambda runtime must be Python 3.12+ (Amazon Linux 2023 with glibc 2.34)
# Python 3.11 and earlier use Amazon Linux 2 with glibc 2.26, which is incompatible
# Development dependencies (optional for testing)
# pytest==7.4.0
# moto==4.2.0